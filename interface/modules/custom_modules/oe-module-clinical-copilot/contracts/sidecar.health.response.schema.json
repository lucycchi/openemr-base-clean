{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open-emr.org/clinical-copilot/contracts/sidecar.health.response.schema.json",
  "title": "sidecar GET /health response",
  "description": "Liveness of the sidecar process: it answers, and with which prompt and parser versions. No dependencies are checked; that is /ready. Reachable only on the docker network (PHP's ready.php reports it to the outside).",
  "type": "object",
  "properties": {
    "status": {
      "const": "ok"
    },
    "prompt_version": {
      "type": "string",
      "minLength": 1
    },
    "model": {
      "type": "string",
      "minLength": 1
    },
    "parser": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "status",
    "prompt_version",
    "model",
    "parser"
  ],
  "additionalProperties": false
}
