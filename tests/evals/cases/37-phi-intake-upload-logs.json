{
  "id": "37-phi-intake-upload-logs",
  "guards": "invariant",
  "theme": "authorization",
  "live": true,
  "failure_mode": "An intake form carries a name, a date of birth, a phone number and a chief concern: none may appear in logs or traces; the mismatch flag is recorded as a fixed phrase only; the sidecar's log lines for the same document must pass the same scan and every one must carry the request's correlation id, so the run can be reconstructed from logs alone.",
  "mode": "phi_logs",
  "fixture": "intake-full.pdf",
  "doc_type": "intake_form",
  "truth": "intake-full.truth.json",
  "model_output": "intake-full.model.json",
  "phi": [
    "/demographics/name",
    "/demographics/dob",
    "/demographics/phone",
    "/chief_concern",
    "/medications/0/name"
  ],
  "rubrics": {
    "no_phi_in_logs": true,
    "factually_consistent": true
  },
  "expect": {
    "status": "extracted"
  }
}
